{
  "pluginAlias": "jqx00",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "jqx00"
      },
      "topic": {
        "title": "DeviceToken",
        "type": "string",
        "required": true,
        "description": "Unique token of the device."
      },
      "model": {
        "title": "Model",
        "type": "string",
        "oneOf": [
          { "title": "JQ200", "enum": ["JQ200"] },
          { "title": "JQ300", "enum": ["JQ300"] }
        ]
      }
    }
  }
}